# STE, PS connection control
type psccd, domain;
type psccd_exec, exec_type, file_type;
init_daemon_domain(psccd)

allow psccd self:process execmem;
allow psccd self:socket read;
allow psccd stercd:unix_dgram_socket sendto;
